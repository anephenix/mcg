const lib = require('../../lib');
describe('lib', () => {
	lib;
	describe('#createRequiredFolders', () => {
		it.todo(
			'should create a set of folders that need to exist for the files, unless they exist'
		);
	});

	describe('#createRequiredFiles', () => {
		it.todo('should create a set of files based on the name of the model');
	});
});
