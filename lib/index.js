const { createRequiredFolders } = require('./createRequiredFolders');
const { createRequiredFiles } = require('./createRequiredFiles');

module.exports = {
	createRequiredFolders,
	createRequiredFiles,
};
